import { createTheme } from "@material-ui/core/styles";

const GCTheme = createTheme({
    palette: {
        primary: {
            main: "#037dbf",
            contrastText: "#ffffff"
        },
        secondary: {
            main: "#e53333"
        }
    }
});

export default GCTheme;